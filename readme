Installation: 
1. helm repos hinzufügen: helm repo add jupyterhub https://hub.jupyter.org/helm-chart/
                          helm repo update
                          helm repo add ngshare https://libretexts.github.io/ngshare-helm-repo/
                          helm repo update
2. install.sh ausführen 
3. Moodle Aktivität erstellen
